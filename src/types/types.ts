export interface project {
  name: string;
  link: string;
  description: string;
  techStack: Array<string>;
}

export interface link {
  name: string;
  link: string;
}

export interface stack {
  title: string;
  stack: Array<String>;
}
